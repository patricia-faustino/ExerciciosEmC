#include <iostream>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char** argv) {
	int numero1 = 2, numero2 = 3 , resultado = 0;
	
	resultado = A + B;
	
	resultado = A - B;
	
	resultado = A * B;
	
	resultado = A / B;
	
	resultado = (A + B) / 3;
	
	return 0;
}
