#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	char caractereQualquer = '@';
	char stringQualquer[30] = "Patricia";
	int inteiroQualquer = 22;
	float floatQualquer = 23.5;
	double doubleQualquer = 24.55;
	bool booleanoQualquer = true;
	//enum : dados enumerados 
	//void: ausência de valores, inicialmente ocupam 0 bits em memória
	// pointer: localização de memória
	long int inteiroMaiorQualquer = 22; // aumenta a capacidade de armazenamento
	unsigned floatPositivoQualquer = 22.5; // duplica a faixa de valores mas não permite números negativos
	short int inteiroMenorQualquer = 2; // reduz a capacidade de armazenamento
	
	
	return 0;
	
}
